import { makeStyles } from "@material-ui/core";

const HomeStyle = makeStyles( {
    main: {
        padding: '110px 0 0 0',
        color: 'white',
        textAlign: 'center',
    },
})

export default HomeStyle;